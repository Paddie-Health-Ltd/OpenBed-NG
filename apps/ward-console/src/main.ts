// PLACEHOLDER. The UI is blocked on a decision — see the commit message.
// scripts/lint_no_service_role_in_bundle.sh reds the moment @supabase/supabase-js
// is imported, on its own JSDoc and a prefix predicate rather than on any
// credential. Narrowing a security pattern to accommodate a dependency is not a
// change to make quietly.
export const placeholder = true;
