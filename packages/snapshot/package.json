{
  "name": "@openbed/snapshot",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "The snapshot codec. One implementation of the payload encoding, built from packages/fixtures/snapshot-shape.json at module load, so neither the generator nor the dashboard carries a column list.",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  }
}
