# Handoff — 2026-09-09

State of OpenBed-NG at the end of the Bundle 1 sessions. Written because none of
the reasoning below survives a dead conversation, and several of the decisions
read as arbitrary without it.

**Where things stand:** `main` is `9337f1f`, CI green on the merge commit (run
`34409361692`, all six jobs `success`), 304 tests ZERO-RED. Bundle 1 and the
merge-blocking Bundle 7 slice are built, merged and verified. **Nothing has been
applied to the hosted project yet.**

---

## 1. Outstanding, in order

| # | Item | Owner |
|---|---|---|
| 1 | ~~**Disable legacy API keys**~~ — **DONE 2026-09-09.** The exposed legacy `service_role` JWT is dead. Legacy `anon` died with it: same signing secret. | Founder, dashboard |
| 2 | Prove the old key is dead — `docs/runbook-key-rotation.md` | Founder |
| 3 | Apply migrations to hosted — **closes the 001–013 window permanently** | Founder |
| 4 | Check (a) HTTP half — exposed schemas | Either; needs no secret |
| 5 | Check (a) catalogue half + (b) append-only under non-superuser `postgres` | Founder |
| 6 | Check (c) magic-link single-use | Founder, needs a real mailbox |
| 7 | Four clinician questions (§4) | Founder |
| 8 | Backups/PITR confirmation — runbook §3b | Founder |
| 9 | **Diagnose the `lint_migration_header` ACCEPT-leg intermittent (§8)** — OPEN, cause unknown. **Sunset 2026-09-24**, and that date is a commitment to do the work, not to check whether it is still failing | Next session |

The apply is **not** `supabase db push`. `[db.migrations] enabled = false` and
`supabase/migrations/` is absent, so that command would appear to succeed while
leaving hosted empty. Use `scripts/run_migrations.sh`, and treat `--dry-run` as a
**stop condition**: exactly 13 pending, or stop and report.

**Then the apply will report `12 applied this run`, and that is correct.**
Migration 001 is applied and ledgered by a separate bootstrap step, because the
ledger cannot record its own creation; the apply loop counts only 002-013.
Confirm by the ledger (`select count(*) from app.schema_migrations` = 13) and a
second dry run (0 pending), never by the count. Runbook §3 carries the full
reading. Proven against a virgin local database 2026-09-10.

---

## 2. Decisions reversed — two different sets, do not conflate them

### Reversed by the kickoff itself, before any code (pre-existing)

Marked `[REVERSED]` in `Sprint Kickoffs/sprint-kickoff-bedspace-v1-2026-09-08.md`:

- **F3** — freshness cannot be derived from the device clock. Server is the only
  time authority; a cheap Android three hours slow renders a four-hour-old row
  green.
- **A1** — public reads come from a static snapshot, not Realtime or PostgREST.
  Free-tier Realtime refuses connections past 200 concurrent, and a dropped
  WebSocket looks open to JavaScript until a write fails.
- **24-hour staleness ceiling** (partially) — beyond 24h the count is suppressed
  entirely rather than badged grey.

### Reversed during these sessions

- **`blood_status` cut from Sprint 1 entirely.** Four exchanges hardened it —
  gating rules, three clinical conditions, a nullable timestamp — before anyone
  asked whether it should ship. It failed the standing test: nothing said what it
  means, who maintains it, or how often. Same call as the hospital accuracy
  score. The enum was never created; the display-only verdict and its conditions
  are on file for whenever it does ship, and the column must then be
  `app.tri_state`, **not** a new type, so it inherits the duty-flag guards
  instead of escaping them.
- **Region `af-south-1` → `eu-west-1`.** Not a change of mind: **Supabase has no
  African region** (verified against their docs — 17 regions, none African), so
  the original pin cited something that does not exist. Two claims in A4 were
  struck: "keeps data on the continent" was false, and the NDPA s.41 leg **never
  followed from the geographic claim** — s.41 turns on Nigeria versus
  not-Nigeria, so Cape Town was as much a cross-border transfer as Dublin. Do not
  read the correction as losing a protection; there was none.
- **Ward-level identity supersedes A5.** No individual accounts. An auth account
  represents a ward, or a facility for admin, never a natural person.
  `actor_identity_map`, the severing design, the retention-period decision,
  `staff_contact` and the `REFERRER` role all stop existing. The audit log carries
  no identity-bearing column and **no `detail jsonb`** — a column-list guard is
  blind to `detail->>'ip'`, so keeping it would have made the guard vacuous
  against the exact attack it names.

---

## 3. Ward categories — why ten, and what is deliberately not split

`A_AND_E`, `ICU_ADULT`, `ICU_PAEDIATRIC`, `MEDICAL_ADULT`, `PAEDIATRIC`,
`THEATRE`, `SURGICAL`, `MATERNITY`, `NICU`, `SCBU`.

**The kickoff never enumerated these.** It states "all eight categories" as a
Gate 3 contract and names `paediatrician closes NICU/SCBU` as a duty-flag rule,
and lists no values. The set was chosen at implementation time; an audit before
the window closed found two defects in that choice.

- **`ICU` split by age.** Ventilator circuits, tube sizes, drug dosing and nursing
  competency differ. A dispatcher routing a child to a facility showing free ICU
  beds that are adult beds has been handed a false positive — the same defect the
  duty-cover gate exists to prevent. `ICU` was **not** kept to mean adult: a label
  answering a narrower question than the reader asked is the defect being removed.
- **`PAEDIATRIC` added, `GENERAL_MEDICAL` → `MEDICAL_ADULT`.** There was no tile
  at all for a general paediatric admission, which is commoner than a paediatric
  ICU one. Not named `MEDICAL_PAEDIATRIC`: most children's wards take medical
  **and** post-surgical children in one unit, so the narrower name is the same
  defect in the other direction. The asymmetry with `MEDICAL_ADULT` is correct
  because the reality is asymmetric.

**Not split, deliberately:** `THEATRE`, `A_AND_E`, `SURGICAL`, `MATERNITY` are
usually one unit taking both adults and children.

**Recorded residual:** if `PAEDIATRIC` absorbs post-operative children then
`SURGICAL` is now *de facto* adult — the same implicit narrowing one value out.
This cannot be chased to completion by naming, because where wards divide by age
varies by facility. It is clinician question 4.

The male/female medical split stays closed — decided on a **different axis**
(sex, not age), so splitting on age does not reopen it.

**The paediatrician flag now gates four categories** (`NICU`, `SCBU`,
`PAEDIATRIC`, `ICU_PAEDIATRIC`), flagged in both derivation sites as a **default
pending clinician confirmation**. It is transcription of an existing rule, not a
new one. The flag itself is **not** split: most Nigerian tier-2 facilities have no
neonatologist, so a separate neonatal flag would read `UNKNOWN` or `NO`
permanently — a real distinction nobody keeps honest.

---

## 4. Open clinician questions — severities are different, keep them apart

1. Does `ICU` need splitting into adult and paediatric? *(Proceeding on yes.
   Confirmation, not a gate.)* — **SAFETY**
2. What is the **coarsest** duty flag that still prevents a dangerous paediatric
   referral, given a clerk updating it at 2am? *(Not "how many flags are
   clinically correct" — that framing was wrong.)* — **SAFETY**
3. Should blood status gate Theatre and Surgical the way the anaesthetist flag
   does? *(Prior review: no, display-only.)* — fidelity
4. Does the paediatric ward take post-surgical children, or does `SURGICAL` need
   an age split? — fidelity

If the clinician leaves something fused, that closes it. **Hesitation on ICU
blocks.** Two-day time-box: everything already landed stands either way, because
both corrections have the cheap-if-wrong asymmetry — the recovery is
`ALTER TYPE ... ADD VALUE`, the one enum operation that survives a live ledger.

---

## 5. The two probes — what they establish, and what they do not

**Merge-blocking (Clause 5).** A/B/A on PR #1 with branch-protection settings
held constant: six checks green → `CLEAN`; two red → `BLOCKED`; six green →
`CLEAN`. All six had **reported** in every state, so this is not the
checks-pending case, and the return to `CLEAN` rules out a one-way transition.

> **What it does NOT establish:** this is GitHub's *computed* `mergeStateStatus`
> plus independently verified `enforce_admins: true`. It is **not** a merge
> attempt that was refused. **Do not close that gap by attempting a merge while
> red** — the only way to observe an actual refusal is to try the thing whose
> failure mode is a known-bad commit landing on `main`.

**Magic-link single-use.** `app.invite` no longer holds a token, so the `UNIQUE`
constraint that enforced single-use is gone. That property is now **inherited
from Supabase auth, not asserted here** — removing our own enforcement was right;
inheriting someone else's without a probe is the Clause 5 shape. Runbook §5b
carries a four-step empirical probe. **Not yet run.**

---

## 6. Corrections made along the way

Kept because the pattern is the point, not the individual slips.

- **`service_role` key exposed.** `supabase projects api-keys` does not fetch the
  anon key — it dumps every key, including the legacy `service_role` JWT. Blast
  radius measured, not assumed: repository, scratchpad, `~/.supabase` and `.env`
  all grepped clean, so a session transcript was the only surface. Fixed
  structurally: `scripts/get_publishable_key.sh` prints exactly one key, and the
  bare subcommand appears nowhere in the docs.
- **The digest that could not see a mutation.** The first idempotency test used
  structure plus row counts, and called a planted `ON CONFLICT DO UPDATE SET
  ... = now()` idempotent. Now a per-table content hash. The control was wrong on
  its first attempt and the plant is what found it.
- **The plant that never planted.** A `sed` pattern needed a leading space and
  missed the branch actually in use, so an `ON_ERROR_STOP` removal never
  happened. "Guard has a hole" and "plant never landed" produce identical output.
  Now `test-conventions.md` §8.
- **Three phantom cross-file links.** `public-relations.json`,
  `lint_from_allowlist.sh` and an exposed-schemas note each asserted, present
  tense, that `config_drift.test.ts` checked a fixture against the catalogue.
  **None of the three assertions existed.** All are now real imports.
- **A fabricated history, caught before it was written.** A pasteback stated the
  kickoff specified `PAEDIATRIC` and that it was "traded away" for the NICU/SCBU
  split. The kickoff enumerates no categories and no trade occurred. Recorded as
  an omission in the proposed set instead.
- **Two live ACL defects**, found by the suite on first run: `anon` held
  `EXECUTE` on both `SECURITY DEFINER` RPCs (Supabase's `pg_default_acl` grants it
  **by name**, so `REVOKE ... FROM PUBLIC` never applied — a complete RLS bypass),
  and `anon` held `TRIGGER`/`REFERENCES` on all three mirrors.
- **Two CI defects**, found by the first real run: `compliance-tests` never ran
  `npm run build`, so a guard's ACCEPT leg asserted over an absent bundle — jobs
  do not share a workspace; and `--reporter=junit` alone printed nothing, so a
  one-line assertion had to be downloaded as an artefact.

---

## 7. Things that will look wrong and are not

- **`supabase/migrations/` is empty and `[db.migrations] enabled = false`** —
  deliberate. Two runners over one schema corrupts it. `supabase db reset` does
  **not** restore this schema; use `npm run db:reset`.
- **`auto_expose_new_tables = true`** in `supabase/config.toml` — deliberately at
  the cloud default. Hardening it locally would make the RLS negative suite pass
  against a stricter regime than production runs.
- **`[api] max_rows = 1000`** — cloud default on purpose; the 200-row cap is
  enforced in SQL inside the RPCs, and lowering it locally would make the cap test
  pass for the wrong reason.
- **`refusal_note` and `fingerprint` are forbidden column names that exist
  nowhere** — forbidding an absent name catches a *reintroduction*.
- **TypeScript pinned at 5.9.3** — no stable `typescript-eslint` supports TS 7,
  and the duty-flag ESLint rule must actually run.

## 8. OPEN AND UNDIAGNOSED — the `lint_migration_header` ACCEPT-leg intermittent

**Status: not fixed. Cause not found. Do not close this on a green run.**
**Sunset: 2026-09-24.** Stop rule below.

### What happened

`tests/compliance/lint_migration_header.test.ts > a well-formed plant is
accepted` failed on GitHub Actions at commit `91c7108` (run `34410413362`),
reporting only `AssertionError: expected 1 to be +0`. The lint had exited 1,
which in `scripts/lint_migration_header.sh` can happen **only** when a `FAIL:`
line was printed — so a check genuinely rejected a file. Which file, and which
check, is unknown: the assertion discarded the lint's stdout.

The commit under test added **one file, `docs/handoff-2026-09-09.md`, 209 lines**,
and changed nothing else. The lint's inputs were therefore byte-identical to
commit `f03f066`, where the same job passed.

### The decisive experiment

The job was re-run on the **identical commit, with no changes**. It passed, and
all six required checks went green.

Same commit, same inputs, opposite outcomes. The failure is **nondeterministic**.
That is a finding, not a diagnosis.

### Hypotheses excluded, each by a probe rather than by argument

| Hypothesis | How it was excluded |
|---|---|
| BSD vs GNU userland — the `awk`/`grep`/`printf` in the lint | Ran the lint against the same corpus under `ubuntu:24.04` with **mawk 1.3.4**. PASS. Same class as the earlier `mapfile` bash-3.2 finding, so it was the leading candidate; it is not the cause. |
| bash 3.2 (macOS) vs bash 5.2 (CI) | Same container run, GNU bash 5.2.15. PASS. |
| Divergent corpus between local and CI | `git ls-files database/migrations` = 27, on-disk = 27, and the two lists are identical. Working tree clean; local `HEAD` == `origin/handoff-2026-09-09`. |
| Something in PR #3's diff | `git show --stat` — one docs file, 209 insertions, nothing else. |
| A concurrent test writing into the REAL `database/migrations` | Every write in `tests/compliance/` is rooted at a `withScratch` temp dir, including `audit_log_no_identity_columns.test.ts`'s in-place rewrite, whose `target` is `join(root, MIGRATION)`. |
| A lint script using a fixed-name temp file two workers could share | No script under `scripts/` uses `mktemp`, `/tmp`, `TMPDIR` or `$$` at all. |
| Concurrency across the ten compliance files | Full `--project compliance` run inside the Debian container: **134/134 passed**. The single-file run passed too. |
| SIGPIPE from `printf … \| grep -q` under `pipefail` — `grep -q` exits on first match, and a `pipefail` pipeline reports the writer's 141 | Ruled out by mechanism, not by luck: `grep` cannot exit before data arrives, and every banner in this corpus is far below the 64 KiB pipe buffer, so `printf`'s write completes without blocking. Recorded because it is the right shape of explanation and the next reader will think of it. |
| `find` directory-entry order differing between a fresh CI clone and a local directory that has been stable for days | `scripts/lint_migration_header.sh:47` pipes `find` through `sort`, and each file's five checks are independent of its position in the list. `copyMigrations` (`tests/compliance/_scratch.ts:59`) does use an unsorted `readdirSync`, but it copies **every** entry, so order cannot change the resulting tree. Four other lints run `find` unsorted, but only into `grep`/`xargs`, where order affects report sequence and never the verdict. |
| A check comparing against the current date, tripping at a UTC date boundary | Excluded on stronger ground than timing: **nothing in the path reads the clock.** `date` appears in no script under `scripts/`; no file under `tests/` or `packages/` constructs a `Date`, `Date.now`, `toISOString` or `getFullYear`; every `2026-` string in executable code sits inside a comment. See the timings below — the premise did not hold either. |

### The date-boundary premise, corrected

The date hypothesis arrived with the claim that this failed at a UTC date
boundary. **It did not.**

| Run | Created (UTC) | Result |
|---|---|---|
| `34408423408` (`f03f066`) | `2026-09-09T21:42:50Z` | pass |
| `34409361692` (`9337f1f`) | `2026-09-09T21:53:32Z` | pass |
| **`34410413362` (`91c7108`)** | **`2026-09-09T22:05:32Z`** | **fail** — the assertion itself ran at `22:05:50Z` |
| `34414457348` (`ffdc5a8`) | `2026-09-09T22:54:13Z` | pass |

All four sit inside the same UTC day, within about seventy minutes of each other,
and the failure is in the middle of that window — roughly 1h54m short of midnight
UTC. No boundary was crossed by any of them.

This is recorded rather than dropped because **a hypothesis excluded for the
wrong reason is a false fact even when the verdict is right.** It is the same
distinction §4 of `.claude/rules/test-conventions.md` draws between *impossible*
and *possible but declined for a named reason*: someone will reopen this item and
reason from the reason. The mechanism is excluded — but by the absence of any
clock read, not by the timestamps.

### What was changed, and what it is not

Both legs of the trio that assert **acceptance** now attach the lint's stdout to
the assertion, and the ACCEPT leg additionally prints the scratch directory
listing and the exact bytes written for `900_valid.sql`. That separates the three
states `expected 1 to be +0` cannot: a missing file, a wrong file, or a genuinely
nondeterministic lint.

**This is instrumentation, not a fix.** The next occurrence will name the file
and the check in the CI log. Until then the cause is unknown and this item stays
open.

### Stop rule — two triggers, doing two different jobs

The date and the recurrence path answer different questions. Collapsing them into
one is what turns a sunset review into a deferral, so they are kept apart.

#### Trigger A — recurrence. No date attached.

The instrumentation fires whenever it fires; it needs no calendar. On a
recurrence, exactly one of these two, and nothing in between:

- **A Standard O flaky carve-out**, with everything that standard demands and
  nothing less: three fresh-database full-suite runs showing pass and fail with
  no code change; visible quarantine that keeps the test running and reported
  rather than a bare skip; a tracked ticket with a determinism plan; and its own
  sunset date.
- **Or the job is made deterministic**, and this item closes.

**Recurrence is defined by SHAPE, not by file.** The signature is: *a compliance
guard leg that asserts acceptance — ACCEPT or ANTI-VACUITY — goes red in CI and
green locally on the same commit.* Defining it by filename would assume the cause
is file-specific, which is precisely what nobody knows.

**The precondition that keeps this from becoming a bucket:** local-green on the
**identical commit** must be *demonstrated, not presumed*, before a red is
attributed to this item. Without that, any CI red gets swept in on the assumption
that it would pass locally — and then item 9 is the growing quarantine list
Standard O warns about, which is the failure mode resurfacing rather than being
managed.

#### Trigger B — 2026-09-24. A commitment to do the work.

**On that date the diagnostic work happens, whether or not it has recurred.** The
date does not exist to ask "is it still failing?" — a review with only that
question has nothing to decide, and becomes the deferral it was meant to prevent.

Fourteen days, because the binding constraint is **context decay, not run
volume**. The excluded hypotheses in the table above, and the probes that
excluded them, are live understanding right now; in ninety days this section is a
paragraph someone skims on the way to something else.

Run volume was the argument for a longer date, and it does not survive this
item's own standing warning: **six consecutive green runs preceded this failure,
so the next N greens are not evidence of anything.** A green stretch means
nothing regardless of its length, so waiting to accumulate more of one buys
nothing. **Do not close this item on a passing run.**

*Next session remains the intent. The date is the backstop for when that slips.*
